version: "3.9"
services:

  db:
    build:
      context: .
      dockerfile: deployment/db/Dockerfile
    env_file:
      - .env
    ports:
      - "1433:1433"

  api:
    build:
      context: .
      dockerfile: deployment/api/Dockerfile
    volumes:
      - .:/src
    env_file:
      - .env
    ports:
      - "8000:8000"
    depends_on:
      - db

volumes:
  mssqldata: